" Get the defaults that most users want.
source $VIMRUNTIME/defaults.vim

" Change the sign column's color.
highlight! link SignColumn LineNr

set number
set regexpengine=0
syntax on
filetype indent plugin on
